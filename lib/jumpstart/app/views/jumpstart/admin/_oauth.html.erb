<h4>Social Login with OAuth</h4>
link_to "Rails credentials", credentials_docs_path, target: :_blank
 %>.</p>

<section class="space-y-4">
Jumpstart::Omniauth.all_providers.each
 do |gem_name, details| %>
    <div class="form-picker-group toggle">
check_box_tag "configuration[omniauth_providers][]", gem_name, Jumpstart::Omniauth.enabled?(gem_name), id: gem_name
 %>
label_tag gem_name, details[:name]
 %>
    </div>
  <% end %>
</section>
