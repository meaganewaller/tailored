<%= content_for :title, "Outfits ##{@outfit.id}" %>
<%= turbo_stream_from @outfit %>

<div class="container px-4 mx-auto my-8">
  <div class="flex items-center justify-between mb-4">
    <h1 class="h3">
      <%= link_to "Outfits", outfits_path, class: "" %>
      <span class="font-light mx-2">\</span>
      Outfit #<%= @outfit.id %>
    </h1>
    <%= link_to t("outfits.edit"), edit_outfit_path(@outfit), class: "btn" %>
  </div>

  <div class="p-8 dark:border rounded shadow">
    <%= render @outfit %>
  </div>
</div>
